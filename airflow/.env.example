# UID de l'utilisateur hôte, pour les permissions des volumes (id -u sous Linux)
AIRFLOW_UID=50000

# Clé Fernet, à générer avec :
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-west-3
S3_BUCKET=
